# Đảm bảo optionalDependencies (bản native/WASM binary theo platform của
# "@open-spaced-repetition/binding" — optimizer FSRS chính thức) LUÔN được cài,
# kể cả khi biến môi trường NPM_CONFIG_OMIT=optional hoặc NODE_ENV=production bị set
# ở đâu đó trong pipeline build (một nguyên nhân phổ biến khiến optionalDependencies
# bị bỏ qua âm thầm trên CI/CD, kể cả Vercel).
omit=
fund=false
audit=false
